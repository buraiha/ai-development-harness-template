# AIハーネス導入ガイド

このディレクトリは、`AGENTS.md` の共通行動規範を補足する。プロジェクト知識の本体は人間も参照する `README.md` と `docs/` に置き、ここへ重複して詰め込まない。

## 導入手順

1. テンプレートの `AGENTS.md` と `.ai/` を新しいプロジェクトのルートへコピーする。
2. `.ai/project.md` に、AI作業へ影響するプロジェクト固有ルールだけを、実際のコード、設定、テスト、READMEから確認して記入する。
3. 詳しい仕様や設計は `README.md` または `docs/` に記載し、`.ai/project.md` から必要に応じて参照する。
4. プロジェクト固有の設計書やタスク指示は `.ai/` または `.ai/workflows/` に追加する。
5. 導入後、AIが既存のマニフェスト、スクリプト、CI設定、README、テストから検証手段を発見できることを確認する。

推測した仕様や、まだ導入していないツールを確定事項として記載しないこと。秘密値、個人情報、実運用データはこのディレクトリへ保存しないこと。

## ファイルの役割

- `../AGENTS.md`: AIが最初に読む入口と指示の優先順位
- `common.md`: プロジェクトを問わず共通する調査・変更・検証・報告ルール
- `project.md`: AIの変更判断へ直接影響する、最小限のプロジェクト固有ルール
- `workflows/`: 要求整理、設計、実装、テスト、レビューのフェーズ別手順

`workflows/` の全ファイルを毎回読む必要はない。依頼に関係するファイルだけを読み、複数フェーズにまたがる場合は該当するものを組み合わせる。

最小構成から始めたい場合は `AGENTS.md` へ `.ai/common.md` の要点を統合し、末尾へ短い `Project Specific Rules` を置く運用でもよい。このテンプレートは、役割を分けて保守したい場合の展開形である。
